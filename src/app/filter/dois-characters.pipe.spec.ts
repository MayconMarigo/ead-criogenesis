import { DoisCharactersPipe } from './dois-characters.pipe';

describe('DoisCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new DoisCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
